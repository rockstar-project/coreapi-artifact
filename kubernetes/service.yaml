apiVersion: v1
kind: Service
metadata:
  name: coreapi-artifact
  namespace: rockstar
  labels:
    app: coreapi-artifact
spec:
  ports:
  - port: 8080
    protocol: TCP
  selector:
    app: coreapi-artifact